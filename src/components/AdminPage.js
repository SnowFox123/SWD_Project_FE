import React from 'react';

const AdminPage = () => {
  // Lấy role từ Redux store

  // Kiểm tra nếu role không phải là Admin, chuyển hướng đến trang khác
 
  return (
    <div>
      <h1>Admin Dashboard</h1>
      {/* Nội dung cho Admin */}
    </div>
  );
};

export default AdminPage;
