import React from 'react';

const StaffPage = () => {
  // Lấy role từ Redux store

  // Kiểm tra nếu role không phải là Staff, chuyển hướng đến trang khác
 
  return (
    <div>
      <h1>Staff Dashboard</h1>
      {/* Nội dung cho Staff */}
    </div>
  );
};

export default StaffPage;
