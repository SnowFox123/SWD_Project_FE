import React from 'react';

const SupplierPage = () => {
  // Lấy role từ Redux store

  // Kiểm tra nếu role không phải là Supplier, chuyển hướng đến trang khác
 
  return (
    <div>
      <h1>Supplier Dashboard</h1>
      {/* Nội dung cho Supplier */}
    </div>
  );
};

export default SupplierPage;
